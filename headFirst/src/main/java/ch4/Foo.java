package ch4;

class Foo {
  public void go() {
//intentionally doesn't compile
//x can de declared without initializing it but it cannot be used later
//    int x;
//    int z = x + 3; //compiler error!
  }
}